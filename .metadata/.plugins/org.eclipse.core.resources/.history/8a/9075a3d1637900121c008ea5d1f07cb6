/**
 * 
 */
package com.subject17.jdfs.client;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.PrintWriter;
import java.net.ServerSocket;
import java.net.Socket;
import java.util.Scanner;

import com.subject17.jdfs.client.io.Printer;
import com.subject17.jdfs.client.sender;
import com.subject17.jdfs.client.reciever;
/**
 * @author James Hughes
 *
 */
public class UserNode {
	static BufferedReader input;
	
	/**
	 * @param args 
	 */
	public static void main(String[] args) {
		input = new BufferedReader(new InputStreamReader(System.in));
		Scanner inScan = new Scanner(System.in);
		// TODO Auto-generated method stub
		final int port = 27314;
		Printer.println("Do you wish to start a server or client?");
		Printer.println("1) Server");
		Printer.println("2) Client");
		switch(inScan.next().toLowerCase().substring(0,1)) {
			case "s": case "1": (port); break;
			case "c": case "2": createClientListener(port); break;
		}
		
	}
}
